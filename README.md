# mermaind_to_draw_io_compiler
Рисует диаграммы на draw io по коду на mermaind или plunt uml

инструкциия:
1. вайбкодим нашу схему на `mermaind` или `Puml.
2. Запускаем `index.html`
3. вставляем код
4. дальше получается диаграмма, которую мы можем редактировать.
